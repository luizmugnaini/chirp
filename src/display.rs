/// The size of the screen used to be 64 by 32 pixels. This however is pretty
/// small so we shall use some scaling in order to get a bigger display.
const WIDTH: usize = 64;
const HEIGHT: usize = 32;
